#include <stdio.h>

int main(){
	
	// Declare
		int X;
	
	// Input
		scanf("%d", &X);
	
	// Output
		printf("%d plus %d is %d\n", X, X, (X + X));
		printf("minus one is %d\n", (X + X) - 1);
	
	
	return 0;
}
